# _archive — do not use for daily work

Old copies and layout experiments live here so the **root stays clean**.

| Folder | What |
|--------|------|
| `src_duplicate/` | Old cookiecutter `src/` mirror (packages also live at repo root) |

**Working code is at repo root:** `training/`, `features/`, `core/`, etc.

If you need something from here, copy it out carefully — do not develop inside `_archive`.
