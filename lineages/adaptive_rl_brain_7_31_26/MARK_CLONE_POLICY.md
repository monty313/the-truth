# Mark clone as policy (transplant into meta-learn RL)

**Owner voice:** MARK HERE (Mark Montgomery Jr. clone)  
**Body:** multi-pair tutor shell + structure eyes + **optional attention gates**  
**Doc of issues:** `MARK_CLONE_AS_POLICY_ISSUES.md`  
**Lineage only** — never overwrite `models/PROVEN_*.pt`

---

## Identity

I am not a chat costume. I am the **policy stack** that should:

1. See multi-TF structure the way Mark does (HTF bias, LTF trigger, no mindless flip).  
2. Size and die by **runtime** target%/risk% (shell).  
3. Bank when done.  
4. **Not thrash** on chop (attention).  
5. Later: meta only rewires **trust**, not shell laws.

---

## Code map

| Layer | File | Role |
|-------|------|------|
| Eyes (structure) | `day_runner.recommended_action` / `structure_action_at` | HTF → LTF fallback |
| Attention (Mark) | `equity_day.recommended_action` when `mark_clone=True` | confirm / cooldown / max entries |
| Hands (shell) | `equity_day` `_try_open` `_mark_bar` bank/breach | LOCKED |
| Persona (chat) | `agents/MULTI_PAIR_TUTOR_PERSONA.md` + `.grok/skills/multi-pair-tutor` | explainability |
| Meta (future) | UNSEEN G14–G17 | sensor trust + plasticity |
| A/B measure | `compare_mark_clone_attention.py` | clear% / entries / breach |

---

## Dials

| File | Contents |
|------|----------|
| `checkpoints/multi_pair_dials.json` | Shell personality (risk_use, stop, cap, decode) — claim freeze |
| `checkpoints/mark_clone_attention_dials.json` | Attention only — claim default OFF |

---

## Official Sets (Monty / Mark lock — not optional)

| Set | LTF (entry / first) | HTF confirm (last two) | Role |
|----:|---------------------|------------------------|------|
| 1 | **1m** | 15m, 30m | Micro scalp speed |
| 2 | **5m** | 30m, 1h | Intraday |
| 3 | **15m** | 1h, 4h | Swing stack |
| 4 | **30m** | 4h, 1d | Macro gravity |

Code: `perception/sets.py` · scan: `perception/mark_sets_opportunity.py` · eyes: `eyes_mode=mark_all_sets`

**ENTJ rule:** look at **all four** for opportunities. HTF = bias. LTF = trigger.  
Aligned (HTF + LTF same) → take. Pullback (LTF opposite) → wait. Multi-set agree → higher conviction. Conflict / no trigger → HOLD.

**Production note:** `code/features/engine.py` still has an older wrong lock (set2=5m/1h/4h, set3=15m/4h/1d). Lineage Mark eyes use the correct table above. Do not silently re-prove PROVEN on wrong sets.

## How the bot “moves like us”

**Claim path (default, legacy_set2):**  
only Official Set 2 collapsed → open/reverse on that flip → shell.

**Mark eyes path (`eyes_mode=mark_all_sets`):**  
scan Sets 1–4 → fire only on **aligned** opportunities → shell same.

**Mark + thrash caps (optional):**  
confirm / post-stop cooldown / max entries — measured; pure caps hurt clear%. Prefer better eyes + BC over hard max-entries.

---

## Training transplant (next builds)

1. Teacher labels = Mark-gated heuristic actions on practice days.  
2. Channel1 (or larger) BC to teacher + existing anti-hold / second-best regret.  
3. Gate: pure greedy match rate + entries > 0; no promote over claim until score_ten_pairs PASS.  
4. Meta: only after label strata (pullback, conflict) prove which sensors lie on forward.

---

## Hard refusals

- Do not reintroduce trail + cushion + scale-in.  
- Do not fit dials on forward.  
- Do not claim “100-day consistent” with only 90 days.  
- Do not touch PROVEN.
