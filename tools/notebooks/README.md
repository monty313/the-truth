# notebooks

Jupyter / Colab workbooks.

GPU Colab edition still lives in **`GPU_EDITION/`** at repo root  
(kept there so Colab paths in old runbooks keep working).

Prefer new notebooks here under `notebooks/`.
