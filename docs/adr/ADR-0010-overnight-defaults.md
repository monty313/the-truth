# ADR-0010: Overnight autonomous-build defaults (2026-07-19 night)
Monty asleep; blanket 'complete the entire project' given. Calls made + flagged for review:
1) Trace layer = local JSONL spans (Langfuse-spec-compatible), MLflow optional. 2) Heat guard
ON via config (see ADR-0002). 3) Gold CSV staging failed 4x (121MB pipe limit) -> Gauntlet +
smoke training ran on SYNTHETIC gold-like data, clearly labeled; real-data rerun is one
command once the zip lands. 4) Boot-camp GRADUATION not claimed — plumbing proven, bar not.
5) Expansion symbols assumed = EURUSD/GBPUSD/US30 (his data files).
