# Mix recipe — climb above high-water (pack-safe)

**Ban:** full strategy-only multi-head replace (proved 35→15 REJECT).

| Role | Source | Weight / rule |
|------|--------|----------------|
| **PRIMARY** | Multi-day **path-family** BC (`fable_kag_l2l.collect_cluster_labels`) — fire_skill / wait_skill across MWT days | Full batch; not calendar-day memos |
| **PROTECT** | Award-day self-imitate + HOLD-floor inject if hold_frac < 0.35 | heavy HOLD weight |
| **PROTECT** | KL anchor to BEST embryo | kl_coef ≈ 1.0–1.25; **single-head** 168/128 only |
| **AUX** | Strategy fire samples (CCI cont / RSI-BB BUY/SELL only) | ≤ ~8–10% mass; **no** strategy HOLD flood |
| **GATE** | Frozen 50d `score_policy` + baseline mark_rows | KEEP only if same↑ and breach=0 and clear≥27 |

**Architecture:** `Channel1Policy(multi_head=False)` matching `mark_clone_full_obs_v1.pt`.  
**Obs:** 168-dim frozen. **PROVEN:** untouched.
