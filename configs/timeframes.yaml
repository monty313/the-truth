# WHAT: the 4-Set Interlocking Matrix. Monty lock 2026-07-24.
# Primary stacks (LTF + two HTFs): 1m/15m/30m; 5m/1h/4h; 15m/4h/1d
# set4 retained as extra-confidence stack.
# CHANGE LOG:
# - 2026-07-24  set2 HTFs -> 1h,4h; set3 HTFs -> 4h,1d — WHY: Monty exact TF lock. Semantic obs shift.
sets:
  set1: {ltf: 1min,  htfs: [15min, 30min], extra: 1h}
  set2: {ltf: 5min,  htfs: [1h, 4h],       extra: 1d}
  set3: {ltf: 15min, htfs: [4h, 1d],       extra: 1w}
  set4: {ltf: 30min, htfs: [4h, 1d],       extra: 1w}
observation_only: [1w]
