# How pt5 maps into the-truth

**pt5 = principles.** Code = how this lab measures them.  
Canonical full text: `pack/pt5__basic_knowledge.txt`  
ARMY vault owns soul-searchable copy; this folder is the mirror (`SOUL_MATCH.md`).

---

## Three boxes (never merge)

```
SOUL (Mark / free will)     →  ARMY personality + Fable + moral doctrine
        ↓ teaches
DOCTRINE / EYES (pt5 laws)  →  mark_doctrine · sets · soul size/adds
        ↓ promote only if gates pass
PROVEN (frozen champion)    →  models/PROVEN_*.pt  — do not casual overwrite
```

Shell under all trades: **`equity_day`** (bank, floor, heat). Doctrine cannot waive the floor.

---

## Law → lab

| pt5 law | Principle | Lab encoding (T3 lineage) | Production (T1) hint |
|---------|-----------|---------------------------|----------------------|
| 1 Dominant trends | HTF permission; LTF timing only | `MARK_SETS_LAW` · `sets.py` · force gate in `mark_doctrine.py` | `sets_lock: mark` only on new trains; rewards with-trend / against-trend |
| 2 Acceleration | Breath vs launch; two rulebooks | PlayState breather / launch / aligned | `w_pullback_with_htf`, `w_quick_pull_close` |
| 3 Regime | State machine; wrong book = death | Regime enum chop/flat/bull/bear | masks / setup_skip / regime language |
| 4 Capital | Survival first; size from remaining | `equity_day` breach/bank/heat · **mark_soul size dials** | death penalty, heat, daily goal/floor inputs |
| 5 Speed vs weight | Force > velocity | Entry only if side(force)==side(setup) | HTF firm vs LTF pull/cont tags |

**Slingshot (same family):** load on LTF against while HTF intact → fire on resume with HTF → kill if tide breaks → **no average against gravity**.

---

## What pt5 is NOT

| Not pt5 | Where that lives instead |
|---------|--------------------------|
| 92 signal agents | `code/signals/` + `signal_slots.yaml` — **after soul** |
| PROVEN weights | `models/` |
| Specific CCI lengths / BB names | Disposable **instances**; roles (mass vs velocity) stay |
| Chat personality | `MARK_PERSONALITY.md` (ARMY) |

---

## Control chain (exam sentence)

```
FORCE (HTF last two of each set)
  → REGIME (playbook + m_regime)
  → VELOCITY (LTF first: breath vs launch)
  → ENTRY only if sides match + heat/daily risk OK
  → size relative to remaining goal (Mark soul)
  → add only with force if lagging
  → regime shift → flatten / HOLD / m→0
```

Official sets (immutable):

| Set | LTF | HTF |
|----:|-----|-----|
| 1 | 1m | 15m, 30m |
| 2 | 5m | 30m, 1h |
| 3 | 15m | 1h, 4h |
| 4 | 30m | 4h, 1d |

---

## Agent load order (every session in this lab)

1. `00_MAP_OF_THE_HOUSE.md`  
2. `SOUL_MATCH.md`  
3. This index / pt5 pack  
4. `GOAL.md` + track ID (T1–T4)  
5. Only then code for that track
